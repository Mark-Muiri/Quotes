import { PublishedDatePipe } from './published-date.pipe';

describe('PublishedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PublishedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
